import 'package:flutter/material.dart';

class fruits extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(child: Text("This is fruit page"),),
    );
  }
}